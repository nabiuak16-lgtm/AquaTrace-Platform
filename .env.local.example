# OpenAI API key for GPT-powered result analysis (server-side only, never exposed to the client).
# Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional: override the model (default: gpt-4o-mini)
OPENAI_MODEL=
